## Downturn - Matrix and Bit Representation

Like Connect Four, but with a specified -w width, -h height, 
-r run (with an objective to get "run" pieces in a row) and -m or -b to specify 
a matrix or bit representation.
